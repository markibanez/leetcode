# Typescript solutions to LeetCode problems

## Installation
1. `npm install`
2. `npm install -g esr`

## Usage
`esr ./problems/[00XXX] - [problem-name].ts` \
For example, `esr ./problems/00001 - Two Sum.ts`